source "https://rubygems.org"

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

gem "google_maps_service"  # For Google Maps API integration
gem "sqlite3"              # For SQLite database interaction
gem "sinatra"
gem 'puma', '~> 6.0'
gem "rackup"
gem 'sinatra-cors', require: 'sinatra/cors'